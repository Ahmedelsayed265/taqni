{% if description %}
<section class="subTitle-detail">
    <div class="container">
        <div class="d-flex">
            <p class="bread-text">{{ description }}</p>
        </div>
    </div>
</section>
{% endif %}